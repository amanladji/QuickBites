services:
  - type: web
    name: quickbite-frontend
    env: static
    plan: free
    staticPublishPath: .
    buildCommand: echo "No build needed — static site"
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
